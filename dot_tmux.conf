set -g mouse on

# Pass through extended keys (CSI u) so Shift+Enter etc. reach the app
# (needed for newline via Shift+Enter in Claude Code inside tmux)
set -s extended-keys always
set -as terminal-features 'xterm*:extkeys'
